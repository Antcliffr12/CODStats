import React from 'react'

export default function GetAllDataOverview(props) {
    //instead of props.Whatever lets make variables with it
    const data = props;
    console.log(data.data)
    // const lifetime = data.data.lifetime;
    // const mode = lifetime.mode;
    // const brAll = mode.br_all.properties;
        return (
            <div className="container">
                <div className="col-12">
                    
                </div>
           </div>
        )
    
}
